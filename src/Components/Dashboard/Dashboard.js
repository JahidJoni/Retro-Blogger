import React from 'react';
import SideBar from '../SideBar/SideBar'

const Dashboard = () => {
    return (
        <div className="mt-5">
            <SideBar></SideBar>
        </div>
    );
};

export default Dashboard;